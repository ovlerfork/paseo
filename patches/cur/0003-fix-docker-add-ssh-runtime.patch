From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: patch-ci <ci@localhost>
Date: Wed, 1 Jul 2026 12:03:00 +0000
Subject: [PATCH] fix(docker): add optional ssh runtime


diff --git a/docker/base/Dockerfile b/docker/base/Dockerfile
index 405f6049c2065fa1c134310fe6be74bedc8bcd0a..706af6d5f556bef88573609a295442c71d5abe84 100644
--- a/docker/base/Dockerfile
+++ b/docker/base/Dockerfile
@@ -51,6 +51,7 @@ RUN set -eux; \
       lbzip2 \
       less \
       openssh-client \
+      openssh-server \
       pipx \
       python3-venv \
       ripgrep \
@@ -145,7 +146,7 @@ RUN chmod +x /usr/local/bin/paseo-docker-entrypoint
 
 WORKDIR /workspace
 
-EXPOSE 6767
+EXPOSE 6767 2222
 VOLUME ["/home/paseo"]
 
 HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3 \
diff --git a/docker/base/Dockerfile.source b/docker/base/Dockerfile.source
index 7d9d79a4a4da958295b8076d620e3e3ee4cf1bdf..ab6a2996b63d4a1f5debb572e4cced1c79e2f330 100644
--- a/docker/base/Dockerfile.source
+++ b/docker/base/Dockerfile.source
@@ -74,6 +74,7 @@ RUN set -eux; \
       lbzip2 \
       less \
       openssh-client \
+      openssh-server \
       pipx \
       python3-venv \
       ripgrep \
@@ -168,7 +169,7 @@ RUN chmod +x /usr/local/bin/paseo-docker-entrypoint
 
 WORKDIR /workspace
 
-EXPOSE 6767
+EXPOSE 6767 2222
 VOLUME ["/home/paseo"]
 
 HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3 \
diff --git a/docker/base/rootfs/usr/local/bin/paseo-docker-entrypoint b/docker/base/rootfs/usr/local/bin/paseo-docker-entrypoint
index a9138d41f4744b9daa87ac0a980f0aaebeeb85c0..a32c3652ed6dd39e47c6816ab87dd7e8655eb006 100644
--- a/docker/base/rootfs/usr/local/bin/paseo-docker-entrypoint
+++ b/docker/base/rootfs/usr/local/bin/paseo-docker-entrypoint
@@ -33,6 +33,8 @@ home_default PASEO_HOME "/.paseo"
 : "${PASEO_WEB_UI_ENABLED:=true}"
 : "${PASEO_LOG_LEVEL:=info}"
 : "${PASEO_LOG_FORMAT:=json}"
+: "${PASEO_SSH_AUTHORIZED_KEYS_FILE:=/run/configs/paseo_ssh_authorized_keys}"
+: "${PASEO_SSH_PORT:=2222}"
 home_default CLAUDE_CONFIG_DIR "/.claude"
 home_default CODEX_HOME "/.codex"
 home_default XDG_CONFIG_HOME "/.config"
@@ -47,6 +49,8 @@ export PASEO_LISTEN
 export PASEO_WEB_UI_ENABLED
 export PASEO_LOG_LEVEL
 export PASEO_LOG_FORMAT
+export PASEO_SSH_AUTHORIZED_KEYS_FILE
+export PASEO_SSH_PORT
 export CLAUDE_CONFIG_DIR
 export CODEX_HOME
 export XDG_CONFIG_HOME
@@ -75,6 +79,48 @@ ensure_dir "$XDG_DATA_HOME"
 ensure_dir "$XDG_STATE_HOME"
 ensure_dir "$XDG_CACHE_HOME"
 
+start_sshd() {
+  case "${PASEO_SSH_ENABLED:-false}" in
+    true | 1 | yes) ;;
+    false | 0 | no | "")
+      return
+      ;;
+    *)
+      echo "[paseo] FATAL: unsupported PASEO_SSH_ENABLED=${PASEO_SSH_ENABLED}; expected true or false." >&2
+      exit 1
+      ;;
+  esac
+
+  if [[ "$(id -u)" != "0" ]]; then
+    echo "[paseo] FATAL: PASEO_SSH_ENABLED requires the container entrypoint to run as root." >&2
+    exit 1
+  fi
+  if [[ ! -s "$PASEO_SSH_AUTHORIZED_KEYS_FILE" ]]; then
+    echo "[paseo] FATAL: PASEO_SSH_AUTHORIZED_KEYS_FILE is missing or empty: ${PASEO_SSH_AUTHORIZED_KEYS_FILE}" >&2
+    exit 1
+  fi
+
+  local ssh_dir="$HOME/.ssh"
+  install -d -m 700 "$ssh_dir"
+  install -m 600 "$PASEO_SSH_AUTHORIZED_KEYS_FILE" "$ssh_dir/authorized_keys"
+  if [[ "$PASEO_RUNTIME_USER" == "paseo" ]]; then
+    chown -R paseo:paseo "$ssh_dir"
+  fi
+  install -d -m 755 /run/sshd
+  ssh-keygen -A
+
+  echo "[paseo] starting sshd on port ${PASEO_SSH_PORT} for ${PASEO_RUNTIME_USER}"
+  /usr/sbin/sshd \
+    -o AuthorizedKeysFile=.ssh/authorized_keys \
+    -o KbdInteractiveAuthentication=no \
+    -o PasswordAuthentication=no \
+    -o PermitRootLogin=prohibit-password \
+    -o PubkeyAuthentication=yes \
+    -p "$PASEO_SSH_PORT"
+}
+
+start_sshd
+
 if [[ "$#" -gt 0 ]]; then
   if [[ "$(id -u)" == "0" && "$PASEO_RUNTIME_USER" == "paseo" ]]; then
     exec gosu paseo "$@"
diff --git a/docker/docker-compose.example.yml b/docker/docker-compose.example.yml
index 1f1d5cd5325d7196ed3dcf2f6ef7d3abbf640199..5657d95f29cc3f581fa7c757a95c2e7bf3e47c14 100644
--- a/docker/docker-compose.example.yml
+++ b/docker/docker-compose.example.yml
@@ -9,13 +9,24 @@ services:
     restart: unless-stopped
     ports:
       - "6767:6767"
+      # Uncomment to expose the optional key-only SSH daemon.
+      # - "2222:2222"
     environment:
       PASEO_PASSWORD: "change-me"
       # Add DNS names you use to reach this container. IPs and localhost are
       # already allowed by default.
       # PASEO_HOSTNAMES: "paseo.example.com,.lan"
+      # PASEO_SSH_ENABLED: "true"
+      # PASEO_SSH_AUTHORIZED_KEYS_FILE: "/run/configs/paseo_ssh_authorized_keys"
     volumes:
       # Persistent daemon state and agent credentials/config.
       - ./paseo-home:/home/paseo
       # Code visible to Paseo and the agents it launches.
       - ./workspace:/workspace
+    # configs:
+    #   - source: paseo_ssh_authorized_keys
+    #     target: /run/configs/paseo_ssh_authorized_keys
+
+# configs:
+#   paseo_ssh_authorized_keys:
+#     file: ./paseo_ssh_authorized_keys.pub
diff --git a/docs/docker.md b/docs/docker.md
index 160a2e9fd3d686fe9c08105eeced923abf55f4e0..f4314ad2b26d71b107b13e78043b555478473346 100644
--- a/docs/docker.md
+++ b/docs/docker.md
@@ -69,6 +69,49 @@ services:
       - ./workspace:/workspace
 ```
 
+### Optional SSH Access
+
+The image can start an SSH daemon for direct shell access to the same runtime
+user that runs Paseo. SSH is disabled by default and supports key-based access
+only; password authentication is not enabled.
+
+Write your public key to a local file:
+
+```bash
+cp ~/.ssh/id_ed25519.pub paseo_ssh_authorized_keys.pub
+```
+
+Then enable SSH in Compose with a config mount:
+
+```yaml
+services:
+  paseo:
+    ports:
+      - "6767:6767"
+      - "2222:2222"
+    environment:
+      PASEO_PASSWORD: "change-me"
+      PASEO_SSH_ENABLED: "true"
+      PASEO_SSH_AUTHORIZED_KEYS_FILE: "/run/configs/paseo_ssh_authorized_keys"
+    configs:
+      - source: paseo_ssh_authorized_keys
+        target: /run/configs/paseo_ssh_authorized_keys
+
+configs:
+  paseo_ssh_authorized_keys:
+    file: ./paseo_ssh_authorized_keys.pub
+```
+
+Connect as the runtime user:
+
+```bash
+ssh -p 2222 paseo@localhost
+```
+
+The SSH port can be changed with `PASEO_SSH_PORT`. If you build or run the
+image with `PASEO_RUNTIME_USER=root`, connect as `root`; root SSH login still
+requires a key.
+
 ## Installing Agents
 
 The base image does not preinstall Claude Code, Codex, OpenCode, Copilot, Pi, or
@@ -121,11 +164,14 @@ or `compose.environment`; Paseo passes them to launched agents.
 
 The image defaults:
 
-| Variable       | Default              |
-| -------------- | -------------------- |
-| `HOME`         | `/home/paseo`        |
-| `PASEO_HOME`   | `/home/paseo/.paseo` |
-| `PASEO_LISTEN` | `0.0.0.0:6767`       |
+| Variable                         | Default                                  |
+| -------------------------------- | ---------------------------------------- |
+| `HOME`                           | `/home/paseo`                            |
+| `PASEO_HOME`                     | `/home/paseo/.paseo`                     |
+| `PASEO_LISTEN`                   | `0.0.0.0:6767`                           |
+| `PASEO_SSH_ENABLED`              | `false`                                  |
+| `PASEO_SSH_AUTHORIZED_KEYS_FILE` | `/run/configs/paseo_ssh_authorized_keys` |
+| `PASEO_SSH_PORT`                 | `2222`                                   |
 
 If you bind-mount host directories on Linux, make sure the container user can
 write them. The built-in `paseo` user has uid/gid `1000:1000`. For a different
@@ -176,6 +222,8 @@ IPs and `localhost` are allowed by default.
 ## Security
 
 - Set `PASEO_PASSWORD` for any published port or network-reachable deployment.
+- Leave SSH disabled unless you need shell access. When enabled, publish it only
+  on trusted networks and use authorized keys rather than passwords.
 - Prefer HTTPS at the reverse proxy for direct browser access.
 - Use the Paseo relay for untrusted networks or mobile access when you do not
   want to expose the daemon port directly.
diff --git a/public-docs/docker.md b/public-docs/docker.md
index be533e66a2fa73893b83387ec624de564bc6dbf9..61f0317758057764303a32cb6ce470d7a0903a96 100644
--- a/public-docs/docker.md
+++ b/public-docs/docker.md
@@ -65,6 +65,49 @@ Start it:
 docker compose up -d
 ```
 
+### Optional SSH Access
+
+The image can start an SSH daemon for direct shell access to the same runtime
+user that runs Paseo. SSH is disabled by default and supports key-based access
+only; password authentication is not enabled.
+
+Write your public key to a local file:
+
+```bash
+cp ~/.ssh/id_ed25519.pub paseo_ssh_authorized_keys.pub
+```
+
+Then enable SSH in Compose with a config mount:
+
+```yaml
+services:
+  paseo:
+    ports:
+      - "6767:6767"
+      - "2222:2222"
+    environment:
+      PASEO_PASSWORD: "change-me"
+      PASEO_SSH_ENABLED: "true"
+      PASEO_SSH_AUTHORIZED_KEYS_FILE: "/run/configs/paseo_ssh_authorized_keys"
+    configs:
+      - source: paseo_ssh_authorized_keys
+        target: /run/configs/paseo_ssh_authorized_keys
+
+configs:
+  paseo_ssh_authorized_keys:
+    file: ./paseo_ssh_authorized_keys.pub
+```
+
+Connect as the runtime user:
+
+```bash
+ssh -p 2222 paseo@localhost
+```
+
+The SSH port can be changed with `PASEO_SSH_PORT`. If you build or run the
+image with `PASEO_RUNTIME_USER=root`, connect as `root`; root SSH login still
+requires a key.
+
 ## Install agent CLIs
 
 Create a child image for the providers you want available:
@@ -149,6 +192,8 @@ IPs and `localhost` are allowed by default.
 
 Set `PASEO_PASSWORD` for any published port or network-reachable deployment. Use HTTPS at your reverse proxy for browser access outside localhost.
 
+Leave SSH disabled unless you need shell access. When enabled, publish it only on trusted networks and use authorized keys rather than passwords.
+
 The static web UI is public on the daemon origin. The daemon API and WebSocket are protected by password auth when configured.
 
 Agents can access whatever you mount into `/workspace` and whatever credentials you place in `/home/paseo`. Keep those mounts scoped to what the agents should be able to use.
